package:
  name: github-actions-test-package
  version: "0.0.1"

build:
  number: 0
  noarch: python
  script: echo "Test package installed"

requirements:
  host:
    - python >=3.10
  run:
    - python >=3.10

test:
  commands:
    - echo "Test passed"

about:
  homepage: https://github.com/anaconda/github-actions
  license: BSD-3-Clause
  summary: Test package for validating the build-package GitHub Action
